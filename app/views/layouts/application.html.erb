<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title(yield(:title)) %></title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
      <nav class="navbar navbar-inverse navbar-fixed-top">
      <div class="container-fluid">
        <div class="navbar-header">
        <%= link_to "Sample App", root_path, id: "logo", class: "navbar-brand" %>
        </div>
        <ul class="nav navbar-nav navbar-right">
          <li><%= link_to "Home",   root_path , class: "activite"%></li>
         <li><%= link_to "Help",   help_path %></li>
         <% if logged_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <%= current_user.name %> <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "Profile", current_user %></li>
              <li><%= link_to "Settings", '#' %></li>
              <li class="divider"></li>
              <li>
                <%= link_to "Log out", logout_path, method: "delete" %>
              </li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to "Log in", login_path %></li>
        <% end %>
        </ul>
      </div>
      </nav>
      <div class="container">
        <% flash.each do |key, msg| %>
          <div class="alert alert-<%= key %>">
            <strong>
              <%= msg %>
            </strong> 
          </div>
        <% end %>
        <%= yield %>
      </div>
      <div class="footer">
        <div class="container">
          <nav>
            <ul>
              <li><%= link_to "About",   about_path %></li>
              <li><%= link_to "Contact", contact_path %></li>
              <li><a href="http://news.railstutorial.org/">News</a></li>
            </ul>
          </nav>
          <h1 class="display-3">This create by <%= link_to "The Tien", "https://www.fb.com/dthtien", target: "_blank" %> acording to Rails tutorials</h1>
        </div>
          <%= debug(params) if Rails.env.development? %>
      </div>
  </body>
</html>
